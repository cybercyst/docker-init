title: Pyproject.toml Docker Bootstrap
version: v0.0.1
schema:
  type: object
  properties:
    app:
      type: string
    port:
      type: integer
    ignore_files:
      type: array
      items:
        type: string
  required:
    - app
steps:
  - source: ./template
