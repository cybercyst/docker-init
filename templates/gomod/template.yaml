title: Go Docker Bootstrap
version: v0.0.1
schema:
  type: object
  properties:
    binary:
      type: string
    port:
      type: integer
    ignore_files:
      type: array
      items:
        type: string
  required:
    - binary
steps:
  - source: ./templates
