title: Go Docker Bootstrap
version: v0.0.1
schema:
  type: object
  properties:
    module:
      type: string
    image:
      type: string
    port:
      type: integer
  required:
    - module
    - image
steps:
  - source: ./templates
